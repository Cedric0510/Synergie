rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Deny all access by default — no Cloud Storage usage currently
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
